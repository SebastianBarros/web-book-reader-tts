{
  "name": "web-book-reader-tts",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare Worker proxy for Google Cloud Text-to-Speech, used by web-book-reader's audiobook mode.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250404.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.95.0"
  }
}
